# Prediction Fixture Files

This folder contains CSV fixtures used by prediction API tests.

- `valid_sample.csv`: expected shape and valid values
- `invalid_rows.csv`: schema and value violations for validation checks
