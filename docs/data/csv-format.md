# CSV Format Guide

Required columns:
- mileage
- engine_hours
- fault_codes
- service_history
- usage_patterns

Notes:
- `fault_codes` supports comma/pipe/semicolon separators
- include header row exactly once
- UTF-8 encoding is recommended
