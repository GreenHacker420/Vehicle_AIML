# Responsive Spacing Rules for Predict Layout

- full-width section should use `w-screen` and center internal content
- use constrained inner container for readability
- maintain `16px` minimum side padding on small screens
- avoid clipped card shadows at viewport edges
- keep result section visually anchored to input forms
